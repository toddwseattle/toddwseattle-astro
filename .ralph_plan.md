# Ralph Plan: Blog & Homepage Polish - IMPLEMENTATION PLAN

## Executive Summary

**Status**: 75% Complete (3 of 4 tasks done)
**Remaining**: Final validation and testing
**Focus**: Astro components, TypeScript safety, zero breaking changes

## Completed Tasks ✅

### ✅ Task 1: Blog Hero Images Resized to 50% Width

**File**: `src/pages/writing/[slug].astro`
**Status**: COMPLETED (Line 73)
**Implementation**:

```astro
<div class="w-full md:w-1/2 mx-auto mb-10 overflow-hidden rounded-xl bg-paper-200 dark:bg-graphite-700 aspect-video">
```

**Result**: Mobile=100%, Desktop=50% centered
**Type Safety**: ✅ `cover` is correctly typed as `ImageMetadata | undefined`

### ✅ Task 2: Author Photo on Homepage

**File**: `src/pages/index.astro`
**Status**: COMPLETED (Lines 10, 109-115)
**Implementation**:

- Import added: `import profileImage from '../assets/images/profile.jpg'`
- Image component in Mini bio section with responsive sizing
- Layout: flex container with image on left, text on right
  **Type Safety**: ✅ `profileImage` typed as `ImageMetadata` via ESM import
  **Asset Verified**: ✅ `src/assets/images/profile.jpg` exists (466KB)

### ✅ Task 3: Resume Page Title Updated

**File**: `src/pages/resume/index.astro`
**Status**: COMPLETED (Line 10)
**Implementation**:

```astro
const title = 'Todd Warren - Resume';
```

**Type Safety**: ✅ String literal, no issues

## Remaining Tasks

### 🔲 Task 4: Comprehensive Validation & Type Checking

**Objective**: Verify all changes are type-safe, performant, and production-ready with focus on Astro-specific concerns.

#### 4.1 TypeScript & Astro Type Checking

**Command**: `npx astro check`
**Validates**:

- TypeScript errors in `.astro` frontmatter
- Component prop types
- Content collection schema compliance
- Image reference validity (build-time checks)
- Missing imports
  **Success Criteria**: Zero errors, zero warnings
  **Type Safety Focus**:
- Verify `ImageMetadata` types are correctly inferred
- Ensure no `any` types introduced
- Check content collection types with `CollectionEntry<'blog'>`

#### 4.2 Production Build

**Command**: `npm run build`
**Validates**:

- Astro SSG compilation succeeds
- Image optimization completes (check `dist/_astro/` for optimized images)
- No missing assets or broken references
- Content collections properly processed
  **Success Criteria**: Clean build with zero errors
  **Astro-Specific Checks**:
- Verify optimized images generated for profile.jpg
- Check hero images are properly optimized
- Ensure build-time data fetching (`getCollection`) works

#### 4.3 Development Server Testing

**Command**: `npm run dev`
**Browser Tests**:

1. **Blog Post Hero Images** (`/writing/[any-post-with-cover]`):
   - Mobile (<768px): Verify hero image is 100% width
   - Desktop (≥768px): Verify hero image is 50% width, centered
   - Check image loading (should be `loading="eager"`)
   - Verify aspect ratio maintained (`aspect-video`)
   - Test dark mode styling

2. **Homepage Author Photo** (`/`):
   - Verify profile image appears in Mini bio section
   - Check responsive sizing (w-24 h-24 → w-32 h-32)
   - Verify `rounded-full` styling
   - Test layout: image left, text right on desktop; stacked on mobile
   - Check image sharpness and optimization

3. **Resume Page** (`/resume/`):
   - Verify browser tab title shows "Todd Warren - Resume"
   - Check `<title>` tag in page source
   - Verify OpenGraph metadata if applicable

**Astro Image Optimization Verification**:

- Inspect Network tab: verify images served in modern formats (WebP/AVIF)
- Check responsive image srcset generation
- Verify lazy loading behavior (except eager-loaded hero images)

#### 4.4 Production Preview Testing

**Command**: `npm run preview`
**Purpose**: Test production build locally before deployment
**Tests**:

- All tests from 4.3 repeated on production build
- Verify image optimization in production mode
- Check bundle sizes and asset loading
- Verify no console errors

#### 4.5 Responsive & Accessibility Testing

**Breakpoints**:

- Mobile: 375px, 414px
- Tablet: 768px, 1024px
- Desktop: 1280px, 1920px

**Accessibility Checks**:

- ✅ Alt text on profile image: "Todd Warren profile photo"
- ✅ Alt text on hero images: Uses blog post title
- Verify semantic HTML structure maintained
- Check keyboard navigation (if interactive elements exist)

#### 4.6 Performance Testing (Optional but Recommended)

**Tools**: Lighthouse, WebPageTest
**Metrics**:

- LCP (Largest Contentful Paint): Should improve with 50% hero images
- CLS (Cumulative Layout Shift): Verify images don't cause layout shift
- Image optimization scores

## TypeScript Safety Checklist

### Content Collections

- [x] Blog collection schema defines `cover: image().optional()` (Line 17, config.ts)
- [x] All blog posts use `CollectionEntry<'blog'>` type
- [x] Zod validation ensures runtime type safety

### Image Imports

- [x] Profile image imported via ESM: `import profileImage from '../assets/images/profile.jpg'`
- [x] Type inferred as `ImageMetadata` automatically
- [x] Used with Astro `<Image>` component correctly

### Component Props

- [x] Blog post layout receives typed `post` prop: `Props { post: CollectionEntry<'blog'> }`
- [x] Cover image destructured with correct optional type
- [x] No type assertions or `any` types used

### Build-Time Safety

- [x] Image paths validated at build time (missing images = build failure)
- [x] Content collection queries use `getCollection()` with type inference
- [x] Top-level `await` used correctly in frontmatter (build-time execution)

## Astro-Specific Implementation Notes

### Islands Architecture

✅ **No JavaScript Shipped for These Changes**:

- Blog hero resize: Pure CSS (Tailwind classes)
- Homepage photo: Static image component (SSR)
- Resume title: Static string
- No `client:*` directives needed

### Image Optimization

✅ **Proper Astro Image Usage**:

```astro
<Image
  src={profileImage}        // ESM import (ImageMetadata)
  alt="Todd Warren profile photo"
  width={128}
  height={128}
  class="w-24 h-24 md:w-32 md:h-32 rounded-full object-cover flex-shrink-0"
/>
```

**Benefits**:

- Automatic WebP/AVIF generation
- Responsive srcset creation
- Build-time optimization
- No runtime JavaScript for images

### Content Collections Best Practices

✅ **Type-Safe Queries**:

```astro
const { post } = Astro.props;           // Type: CollectionEntry<'blog'>
const { title, description, date, tags, cover, category } = post.data;  // Typed properties
const { Content } = await post.render();  // Typed render function
```

### Tailwind Integration

✅ **Design System Compliance**:

- Uses existing color tokens: `paper-200`, `graphite-700`, `ink-800`
- Responsive utilities: `md:w-1/2`, `md:flex-row`
- Follows mobile-first approach
- No custom CSS needed

## Testing Strategy

### Phase 1: Type Validation (CRITICAL)

```bash
npx astro check
```

**Expected Output**: "0 errors, 0 warnings"
**If Errors**: Fix type issues before proceeding

### Phase 2: Build Validation

```bash
npm run build
```

**Check**:

- Build completes successfully
- Inspect `dist/_astro/` for optimized images
- Verify no missing asset warnings

### Phase 3: Local Development Testing

```bash
npm run dev
# Navigate to http://localhost:4321
```

**Test Matrix**:
| Page | Test | Expected Result |
|------|------|-----------------|
| `/writing/[slug]` | Hero image width | 50% on desktop, 100% mobile |
| `/writing/[slug]` | Image centered | `mx-auto` centers image |
| `/` | Profile photo | Visible in Mini bio, rounded |
| `/` | Profile layout | Flex row on desktop, column mobile |
| `/resume/` | Page title | "Todd Warren - Resume" |

### Phase 4: Production Preview

```bash
npm run preview
# Navigate to http://localhost:4321
```

**Repeat all Phase 3 tests**
**Additional Checks**:

- Network tab: Image formats (WebP/AVIF)
- Network tab: Image sizes (verify optimization)
- Console: Zero errors

## Success Criteria (Final Checklist)

### Type Safety ✓

- [ ] `npx astro check` passes with zero errors
- [ ] No TypeScript compilation warnings
- [ ] All image imports correctly typed as `ImageMetadata`
- [ ] Content collection types properly inferred

### Build Success ✓

- [ ] `npm run build` completes without errors
- [ ] Optimized images generated in `dist/_astro/`
- [ ] No missing asset warnings

### Visual Validation ✓

- [ ] Blog hero images appear at 50% width on desktop (≥768px)
- [ ] Blog hero images appear at 100% width on mobile (<768px)
- [ ] Hero images are centered (`mx-auto`)
- [ ] Homepage displays author photo in Mini bio section
- [ ] Profile image is rounded (`rounded-full`)
- [ ] Profile layout is responsive (flex-row → flex-col)
- [ ] Resume page title is "Todd Warren - Resume"

### Performance ✓

- [ ] Images use modern formats (WebP/AVIF in production)
- [ ] Responsive srcset generated for images
- [ ] No layout shift (CLS issues) from image loading
- [ ] Hero images load eagerly (above-fold)

### Accessibility ✓

- [ ] Profile image has descriptive alt text
- [ ] Hero images use blog post title as alt text
- [ ] Semantic HTML structure preserved

### Astro Best Practices ✓

- [ ] Zero client-side JavaScript shipped for these changes
- [ ] Image optimization via Astro Image component
- [ ] Build-time image validation works
- [ ] Content collections queried with proper types

## Risk Assessment

### Low Risk Items ✅

- **Task 1** (Blog hero resize): CSS-only change, no type changes
- **Task 3** (Resume title): String literal change, no dependencies
- **Task 2** (Homepage photo): Additive change, typed import

### Zero Breaking Changes ✅

- No route changes (AGENTS.md requirement)
- No content collection schema changes
- No prop interface changes
- No removal of existing functionality

### Rollback Strategy

**Git-safe changes**: All modifications are version-controlled
**Rollback command**: `git revert <commit-hash>`
**No database migrations**: No data changes required
**No API changes**: No external dependencies affected

## Commands Reference

```bash
# Type checking
npx astro check

# Build validation
npm run build

# Development server
npm run dev

# Production preview
npm run preview

# Code formatting (run before commit)
npm run format

# Full test suite (if available)
npm run test
npm run test:e2e
```

## Implementation Log

### Completed (Previous Sessions)

- ✅ 2024-XX-XX: Blog hero images resized to 50% width
- ✅ 2024-XX-XX: Author photo added to homepage Mini bio
- ✅ 2024-XX-XX: Resume page title updated

### Remaining (This Session)

- 🔲 Run `npx astro check` and resolve any type errors
- 🔲 Run `npm run build` and verify clean build
- 🔲 Test in development mode (`npm run dev`)
- 🔲 Test in production preview (`npm run preview`)
- 🔲 Document final verification results

## Next Steps

1. **Execute Task 4.1**: Run `npx astro check`
   - If errors: Fix type issues
   - If warnings: Review and address
   - Expected: Zero errors/warnings

2. **Execute Task 4.2**: Run `npm run build`
   - Verify successful build
   - Check `dist/_astro/` for optimized images
   - Note any warnings

3. **Execute Task 4.3**: Run `npm run dev`
   - Test all three pages
   - Verify responsive behavior
   - Check dark mode

4. **Execute Task 4.4**: Run `npm run preview`
   - Repeat Task 4.3 tests in production mode
   - Verify image optimization

5. **Document Results**: Update this plan with results
   - Mark success criteria as complete
   - Note any issues found
   - Record final verification timestamp

## Astro Architecture Notes

### Component Structure

```
src/pages/writing/[slug].astro    ← Dynamic route with getStaticPaths()
  ├── Frontmatter (build-time)
  │   ├── import { Image } from 'astro:assets'
  │   ├── getCollection('blog')   ← Type-safe query
  │   └── post.render()            ← Typed Content component
  └── Template (SSR)
      └── Conditional hero image render
```

### Type Flow

```
Content Collection Schema (Zod)
  ↓
Runtime Validation (build-time)
  ↓
TypeScript Types (CollectionEntry<'blog'>)
  ↓
Component Props (type-safe destructuring)
  ↓
Template Rendering (type-checked)
```

### Image Optimization Flow

```
ESM Import (src/assets/images/profile.jpg)
  ↓
Type Inference (ImageMetadata)
  ↓
Astro Image Component
  ↓
Build-time Optimization
  ↓
Multiple Format Generation (WebP, AVIF, fallback)
  ↓
Responsive Srcset
  ↓
Optimized Output (dist/_astro/)
```

## References

### Project Documentation

- **AGENTS.md**: Agent instructions and project guidelines
- **Style Guide**: `/docs/04-style-guide.md`
- **Copilot Instructions**: `.github/copilot-instructions.md`

### Astro Documentation

- [Astro Images](https://docs.astro.build/en/guides/images/)
- [Content Collections](https://docs.astro.build/en/guides/content-collections/)
- [TypeScript in Astro](https://docs.astro.build/en/guides/typescript/)

### Design System

- **Tailwind Config**: `tailwind.config.js`
- **Color Tokens**: paper, ink, graphite (see config)
- **Typography**: Inter (body), JetBrains Mono (code)

---

**Last Updated**: 2025-02-04
**Plan Status**: Implementation Complete, Validation Pending
**Next Action**: Execute Task 4.1 (`npx astro check`)
